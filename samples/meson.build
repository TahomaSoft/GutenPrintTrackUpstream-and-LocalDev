sample_files = [
  'colorbars4.png', 'colorsweep.png',  'profile.jpg',
]

image_samples_dir= '/tmp' / 'samples' / 'samples'

install_data(
  sample_files,
  install_dir :
    get_option('datadir') / meson.project_name() / 'samples'
)
