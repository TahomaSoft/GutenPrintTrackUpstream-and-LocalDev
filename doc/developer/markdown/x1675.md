::: NAVHEADER
  The Developer\'s Guide to Gutenprint                       
  -------------------------------------- ------------------- -----------------------------------
  [Prev](x961.html){accesskey="P"}        Chapter 5. ESC/P2    [Next](c1723.html){accesskey="N"}

------------------------------------------------------------------------
:::

::: sect1
# [5.3. Appropriate Remote Commands]{#AEN1675} {#appropriate-remote-commands .sect1}

All of the remote commands described above are wrapped up with the usual
boilerplate. The files always start with `00 00 00`{.literal} and the
"magic" command described above, then two `ESC @`{.literal}s to reset
the printer. The remote command sequences come next; if they print
anything that is usually followed by a `FF`{.literal} (`0C`{.literal}
hex) character to feed the page, then the file ends with another two
`ESC @`{.literal}s to get back to the ground state.

An alignment sequence goes like this:

1.  Host uses `DT`{.literal} to print an alignment sheet.

2.  User eyeballs the sheet to see which is the best aligned pattern.

3.  Host sends a `DA`{.literal} command indicating which pattern the
    user chose.

4.  If the user said "realign", meaning he isn\'t done yet, go to step
    1.

5.  We are done: host sends a `SV`{.literal} command and exits.

The sequence used (by the STC 3000, at least) to print from the roll
feed is (with byte count omitted):

::: informalexample
[]{#AEN1699}

``` screen
PM 00 00
SN 00 00 00
EX 00 00 00 00 05 01
ST 00 01
SM 00 02
```
:::

The sequence used by the STP 870 to print on plain paper is

::: informalexample
[]{#AEN1702}

``` screen
PM 00 00
IR 00 03
SN 00 00 01
SN 00 01 00
SN 00 02 01
EX 00 00 00 00 05 00
FP 00 00 00
```
:::

and the job finishes with

::: informalexample
[]{#AEN1705}

``` screen
IR 00 02
LD
```
:::

For different paper type settings on the STP 870, the arguments to
`SN`{.literal} vary. The arguments to the first and third `SN`{.literal}
commands are as outlined in the description of the `SN`{.literal}
command above; the arguments to the second ("platen gap") are
`00 01 01`{.literal} for thick papers ("matte paper---heavyweight",
"photo paper" and "premium glossy photo paper") and `00 01 00`{.literal}
for all others.

For roll-mode printing, the STP 870\'s sequence changes as follows.
`IR`{.literal}\'s arguments become `00 01`{.literal} in the header, and
`00 00`{.literal} after the job, and `EX`{.literal}\'s last argument
changes from `00`{.constant} to `01`{.constant}.

For zero-margin printing on the STP 870, the arguments to `FP`{.literal}
become `00 0xb0 0xff`{.literal}. This moves the origin about 5.5mm to
the left, to a point one tenth of an inch to the left of the left-hand
edge of the paper, allowing printing right up to (and beyond) the edge
of the paper. Some printers (at least the STP 870) include white
absorbent pads at the left margin position and other positions (89mm and
100mm on the STP 870) to soak up ink which misses the edge of the paper.
Printing off the edge of paper of a width not aligned with a pad could
result in making a mess of the inside of the printer and ink getting on
the reverse of the paper.
:::

::: NAVFOOTER

------------------------------------------------------------------------

  ---------------------------------- ----------------------------------- -----------------------------------
  [Prev](x961.html){accesskey="P"}    [Home](index.html){accesskey="H"}    [Next](c1723.html){accesskey="N"}
  Remote Mode Commands                 [Up](c463.html){accesskey="U"}            Weaving for inkjet printers
  ---------------------------------- ----------------------------------- -----------------------------------
:::
